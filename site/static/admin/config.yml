backend:
  name: git-gateway
  repo: johnatasjmo/logisticaguru
  branch: master # Branch to update (optional; defaults to master)
publish_mode: editorial_workflow
media_folder: "site/static/img/{{year}}/{{month}}" # Media files will be stored in the repo under images/uploads
collections:
  - name: "blog" # Used in routes, e.g., /admin/collections/blog
    label: "Blog" # Used in the UI
    folder: "site/content/blog" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "author", name: "author", widget: "string"}
      - {label: "categories", name: "categories", widget: "string"}
      - {label: "date", name: "date", widget: "datetime"}
      - {label: "description", name: "description", widget: "string"}
      - {label: "featured", name: "featured", widget: "string"}
      - {label: "featuredalt", name: "featuredalt", widget: "string"}
      - {label: "featuredpath", name: "featuredpath", widget: "string"}
      - {label: "linktitle", name: "linktitle", widget: "string"}
      - {label: "title", name: "title", widget: "string"}
